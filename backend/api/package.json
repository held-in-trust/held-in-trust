{
  "name": "@held-in-trust/api",
  "version": "0.1.0",
  "private": true,
  "description": "GraphQL/REST API over the indexed cap-table state, plus the disclosure-report generator.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
