{
  "name": "@held-in-trust/indexer",
  "version": "0.1.0",
  "private": true,
  "description": "Soroban event indexer keeping the Postgres cap-table in sync with on-chain compliant_token state.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
