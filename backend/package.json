{
  "name": "@held-in-trust/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Indexer + API workspace for the Held in Trust issuer backend.",
  "license": "MIT",
  "type": "module",
  "workspaces": [
    "indexer",
    "api"
  ],
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p indexer/tsconfig.json && tsc --noEmit -p api/tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.64.0",
    "vitest": "^2.1.0"
  }
}
